- id: UC-001
  title: Diagnose local permissions
  requires:
    platform: macos
    permissions:
      - accessibility
      - screen-recording
  steps:
    - check permission status
    - return missing permission hints
  success:
    - result includes permission status
    - missing permissions are explicit

- id: UC-010
  title: List apps and capabilities
  requires:
    platform: macos
  steps:
    - list running apps
    - load app capability registry
    - return support level per app
  success:
    - result includes app names
    - result includes support level

- id: UC-020
  title: Open app and read window state
  requires:
    platform: macos
    permissions:
      - accessibility
  steps:
    - open app
    - list windows
    - read app state
  success:
    - target app is found
    - at least one window or clear empty-window reason is returned

- id: UC-030
  title: Click a visible button in a local app
  requires:
    platform: macos
    permissions:
      - accessibility
  steps:
    - open app
    - read app state
    - find button by role and name
    - click button
    - read app state again
  success:
    - action result is ok
    - trace contains observation action and result
    - app state changed or verifier confirms click effect

- id: UC-040
  title: Type into a visible input field in a local app
  requires:
    platform: macos
    permissions:
      - accessibility
  steps:
    - open app
    - read app state
    - find input by role
    - set value or click and type
    - read app state again
  success:
    - action result is ok
    - input text is visible or verifier confirms value

- id: UC-050
  title: Use browser-harness for browser interaction
  requires:
    platform: any
    services:
      - browser-harness
  steps:
    - connect to browser-harness
    - read browser state
    - click browser element
  success:
    - browser-harness result is ok
    - browser trace event is recorded

- id: UC-060
  title: Block protected targets
  requires:
    platform: macos
  steps:
    - request action against protected app
    - evaluate policy
  success:
    - action is blocked before execution
    - policy decision is returned

- id: UC-070
  title: Record trace for every action
  requires:
    platform: any
  steps:
    - start run
    - perform fake observation
    - perform fake action
    - record result
  success:
    - trace id is returned
    - trace contains ordered events

- id: UC-080
  title: Explain missing permissions
  requires:
    platform: macos
  steps:
    - detect missing permission
    - return action hint
  success:
    - error code is permission related
    - output includes next action hint

- id: UC-090
  title: Operate basic IDEA UI
  requires:
    platform: macos
    permissions:
      - accessibility
  steps:
    - resolve IntelliJ IDEA capability
    - read app state
    - operate a basic dialog or search field
  success:
    - IDEA support level is automation or generic
    - action uses JetBrains-specific or accessibility fallback

- id: UC-100
  title: Search and play Duck in QQ Music
  target:
    kind: app
    id: com.tencent.QQMusicMac
    name: QQ音乐
    platform: macos
  requires:
    platform: macos
    permissions:
      - accessibility
  steps:
    - open app
    - read app state
    - find search input by role or name
    - type 鸭子 into search input
    - press key Enter
    - read search results
    - click result named 鸭子
    - read app state again
  success:
    - QQ音乐 is the target app
    - search query 鸭子 is submitted
    - a result named 鸭子 is clicked
    - trace contains observation action policy and result

- id: UC-101
  title: Search Jay Chou in QQ Music
  target:
    kind: app
    id: com.tencent.QQMusicMac
    name: QQ音乐
    platform: macos
  requires:
    platform: macos
    permissions:
      - accessibility
  steps:
    - open app
    - read app state
    - type 周杰伦 into search input
    - press key Enter
    - read search results
    - click result named 周杰伦
    - read app state again
  success:
    - QQ音乐 is the target app
    - search query 周杰伦 is submitted
    - a result named 周杰伦 is clicked
    - trace contains observation action policy and result

- id: UC-102
  title: Find Jay Chou latest album in QQ Music
  target:
    kind: app
    id: com.tencent.QQMusicMac
    name: QQ音乐
    platform: macos
  requires:
    platform: macos
    permissions:
      - accessibility
      - screenRecording
  goal:
    mode: target
    entity: album
    query: 周杰伦
    constraints:
      artist: 周杰伦
    navigation:
      semanticTabs:
        - 专辑
        - Albums
        - Album
    coverage:
      strategy: scroll-until-stable
      maxScans: 3
      stableObservations: 1
      minObservations: 2
    orderBy:
      field: releaseDate
      direction: desc
    requiredFields:
      - albumName
      - releaseDate
      - artist
    confirmation: detail
    maxIterations: 12
  steps:
    - open app
    - read app state
    - type 周杰伦 into search input
    - press key Enter in search input wait for state change timeout 5s
    - wait for search results to load
    - click tab named 专辑
    - wait for album results to load
    - extract latest Jay Chou album from visible album results; only accept entries where artist is 周杰伦, compare release dates, and return albumName releaseDate artist
  success:
    - QQ音乐 is the target app
    - search query submitted
    - album tab loaded
    - latest Jay Chou album name artist and release date extracted successfully

- id: UC-103
  title: Extract current QQ Music page information
  target:
    kind: app
    id: com.tencent.QQMusicMac
    name: QQ音乐
    platform: macos
  requires:
    platform: macos
    permissions:
      - accessibility
  steps:
    - read app state
    - extract current page information including artist and album details
  success:
    - Current page information extracted successfully

- id: UC-104
  title: Find G.E.M. latest album in QQ Music
  target:
    kind: app
    id: com.tencent.QQMusicMac
    name: QQ音乐
    platform: macos
  requires:
    platform: macos
    permissions:
      - accessibility
      - screenRecording
  goal:
    mode: target
    entity: album
    query: 邓紫棋
    constraints:
      artist: 邓紫棋
    navigation:
      semanticTabs:
        - 专辑
        - Albums
        - Album
    coverage:
      strategy: scroll-until-stable
      maxScans: 3
      stableObservations: 1
      minObservations: 2
    orderBy:
      field: releaseDate
      direction: desc
    requiredFields:
      - albumName
      - releaseDate
      - artist
    confirmation: detail
    maxIterations: 12
  steps:
    - open app
    - read app state
    - type 邓紫棋 into search input
    - press key Enter in search input wait for state change timeout 5s
    - wait for search results to load
    - click tab named 专辑
    - wait for album results to load
    - extract latest G.E.M. album from visible album results; only accept entries where artist is 邓紫棋, compare release dates, and return albumName releaseDate artist
  success:
    - QQ音乐 is the target app
    - search query submitted
    - album tab loaded
    - latest G.E.M. album name artist and release date extracted successfully

- id: UC-120
  title: Find largest file in Downloads
  target:
    kind: app
    id: com.apple.finder
    name: Finder
    platform: macos
  requires:
    platform: macos
    permissions:
      - accessibility
  goal:
    mode: target
    entity: file
    query: Downloads
    coverage:
      strategy: scroll-until-stable
      maxScans: 2
      stableObservations: 1
      minObservations: 2
    orderBy:
      field: fileSize
      direction: desc
    requiredFields:
      - fileName
      - fileSize
    confirmation: list
    maxIterations: 8
  steps:
    - open app
    - read app state
    - extract largest file from visible Downloads file list; compare file sizes and return fileName fileSize
  success:
    - Finder is the target app
    - Downloads file list is observed
    - list coverage is proven by scrolling until stable
    - largest file name and file size are extracted successfully

- id: UC-110
  title: Edit and save a file in Sublime Text
  target:
    kind: app
    id: com.sublimetext.4
    name: Sublime Text
    platform: macos
  requires:
    platform: macos
    permissions:
      - accessibility
  steps:
    - open temp file in Sublime Text
    - read app state
    - dismiss registration dialog if present
    - focus document window
    - "type computer-use-harness: uc-110 into document"
    - press key Command+S
    - verify saved file content
  success:
    - Sublime Text is the target app
    - temp file is opened in Sublime Text
    - registration dialog is dismissed if shown
    - sentinel text is typed into the document
    - saved file exists on disk with exact sentinel content
    - trace contains file-system verification evidence
